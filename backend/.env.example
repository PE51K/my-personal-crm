# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Find these in your Supabase Dashboard: Project Settings

# Project URL (API → Project URL)
SUPABASE_URL=https://your-project.supabase.co

# Service role key (API → service_role key - bypasses RLS, keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database connection string (Database → Connection String → Transaction Mode)
# ONLY needed for initialization container to run migrations
# The backend API uses the Supabase SDK and doesn't need this
SUPABASE_DB_URL=postgresql://postgres.your-ref:your-password@aws-0-region.pooler.supabase.com:6543/postgres

# Storage bucket name for contact photos (optional, default: contact-photos)
SUPABASE_STORAGE_BUCKET=contact-photos

# =============================================================================
# Optional Settings (uncomment to customize)
# =============================================================================
# These all have sensible defaults, but you can override them if needed

# Application environment: development, staging, production
# APP_ENV=development

# Enable debug mode (disable in production)
# APP_DEBUG=true

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# APP_LOG_LEVEL=INFO

# Allowed CORS origins (JSON array format)
# CORS_ORIGINS=["http://localhost:3000"]

# Token expiration times
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Server configuration
# HOST=0.0.0.0
# PORT=8000
